# GitHub 푸시 가이드

## 현재 상태

✅ Git 저장소 초기화 완료  
✅ 파일 추가 완료  
✅ 커밋 완료  
⚠️ 원격 저장소 설정 필요

## 다음 단계

### 1. GitHub에 저장소 생성

1. GitHub에 로그인
2. 새 저장소 생성 (Repository name: `Stable_Diffusion`)
3. 저장소를 생성하되 README, .gitignore, license는 추가하지 않기

### 2. 원격 저장소 연결 및 푸시

```bash
cd /home/01_AI그림생성_Stable_Diffusion

# 원격 저장소 추가 (YOUR_USERNAME을 실제 GitHub 사용자명으로 변경)
git remote add origin https://github.com/YOUR_USERNAME/Stable_Diffusion.git

# 또는 SSH 사용 시
git remote add origin git@github.com:YOUR_USERNAME/Stable_Diffusion.git

# 브랜치 이름을 main으로 변경 (이미 되어있을 수 있음)
git branch -M main

# 푸시
git push -u origin main
```

### 3. 인증 문제 해결

만약 인증 오류가 발생하면:

**Personal Access Token 사용**:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. 새 토큰 생성 (repo 권한 필요)
3. 비밀번호 대신 토큰 사용

**SSH 키 사용**:
```bash
# SSH 키 생성 (없는 경우)
ssh-keygen -t ed25519 -C "your_email@example.com"

# 공개 키를 GitHub에 추가
cat ~/.ssh/id_ed25519.pub
# GitHub → Settings → SSH and GPG keys → New SSH key
```

## 자동 푸시 스크립트

아래 스크립트를 실행하면 자동으로 푸시됩니다:

```bash
#!/bin/bash
cd /home/01_AI그림생성_Stable_Diffusion

# 원격 저장소 URL 입력
read -p "GitHub 저장소 URL을 입력하세요 (예: https://github.com/username/Stable_Diffusion.git): " REPO_URL

# 원격 저장소 설정
git remote remove origin 2>/dev/null
git remote add origin "$REPO_URL"

# 브랜치 이름 확인 및 변경
CURRENT_BRANCH=$(git branch --show-current)
if [ "$CURRENT_BRANCH" != "main" ]; then
    git branch -M main
fi

# 푸시
echo "푸시 중..."
git push -u origin main

echo "완료!"
```

## 확인

푸시 후 GitHub에서 확인:
- https://github.com/YOUR_USERNAME/Stable_Diffusion

## 문제 해결

### "remote origin already exists"
```bash
git remote remove origin
git remote add origin YOUR_URL
```

### "Permission denied"
- Personal Access Token 사용
- 또는 SSH 키 설정

### "Large files detected"
- .gitignore에 큰 파일이 제외되어 있는지 확인
- Git LFS 사용 고려
