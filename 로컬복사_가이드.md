# 📦 Stable Diffusion WebUI 로컬 복사 가이드

## ✅ 기본 답변: **대부분 동일하게 동작합니다!**

프로젝트를 다른 로컬 환경에 복사해도 **거의 동일하게 동작**합니다. 다만 몇 가지 **주의사항**이 있습니다.

---

## 🔍 동일하게 동작하는 이유

### 1. **상대 경로 사용**
프로젝트는 **절대 경로가 아닌 상대 경로**를 사용합니다:

```python
# modules/paths_internal.py 예시
script_path = os.path.dirname(modules_path)  # 상대 경로
models_path = os.path.join(data_path, "models")  # 상대 경로
```

**의미**: 어느 위치에 복사해도 프로젝트 내부 구조는 그대로 유지됩니다.

### 2. **자동 환경 설정**
`webui.sh`와 `launch.py`가 자동으로:
- Python 가상환경 생성/활성화
- 필요한 라이브러리 설치
- 경로 설정

---

## ⚠️ 차이점과 주의사항

### 1. **가상환경(venv) 폴더** - ❌ 복사 불필요

```
현재 크기: 약 5GB
복사 필요: 아니요
```

**이유**:
- 가상환경은 **시스템별로 다를 수 있음** (OS, Python 버전 등)
- 복사해도 새 환경에서 동작하지 않을 수 있음
- `webui.sh` 실행 시 자동으로 새로 생성됨

**권장 방법**:
```bash
# 복사 시 venv 폴더 제외
rsync -av --exclude='venv' source/ destination/
# 또는
tar --exclude='venv' -czf backup.tar.gz stable-diffusion-webui/
```

---

### 2. **모델 파일들** - ✅ 복사 필요

```
현재 크기: 약 8GB
복사 필요: 예 (선택사항)
```

**모델 파일 위치**:
```
models/
├── Stable-diffusion/     # 메인 모델 (필수)
├── VAE/                  # 이미지 압축 모델 (선택)
├── Lora/                 # 스타일 모델 (선택)
└── ...
```

**주의사항**:
- 모델 파일이 없으면 **이미지 생성 불가**
- 하지만 모델은 나중에 다운로드 가능
- 용량이 크므로 네트워크 속도 고려

**권장 방법**:
```bash
# 모델만 별도로 백업
tar -czf models_backup.tar.gz models/
```

---

### 3. **생성된 이미지(outputs/)** - ❌ 복사 불필요

```
복사 필요: 아니요 (선택사항)
```

**이유**:
- 이미 생성된 결과물이므로 필수 아님
- 필요하면 별도로 백업 가능

---

### 4. **캐시 파일(cache/)** - ❌ 복사 불필요

```
복사 필요: 아니요
```

**이유**:
- 자동으로 재생성됨
- 용량만 차지함

---

### 5. **Git 저장소(.git/)** - ⚠️ 선택사항

```
복사 필요: 선택사항
```

**복사하는 경우**:
- Git 히스토리 유지
- 버전 관리 계속 사용

**복사하지 않는 경우**:
- 용량 절약
- 새로 `git clone` 가능

---

### 6. **Python 버전** - ⚠️ 확인 필요

**요구사항**:
- Python 3.10.6 권장 (3.8 ~ 3.11 가능)
- 시스템에 Python이 설치되어 있어야 함

**확인 방법**:
```bash
python3 --version
# 또는
python3.10 --version
```

**차이점**:
- Python 버전이 다르면 일부 라이브러리 호환성 문제 가능
- 하지만 `webui.sh`가 자동으로 처리

---

### 7. **시스템 라이브러리** - ⚠️ 확인 필요

**Linux 필수 패키지**:
```bash
# Debian/Ubuntu
sudo apt install wget git python3 python3-venv libgl1 libglib2.0-0

# Red Hat/Fedora
sudo dnf install wget git python3 gperftools-libs libglvnd-glx
```

**차이점**:
- 시스템 라이브러리가 없으면 실행 실패
- 하지만 에러 메시지로 알려줌

---

### 8. **GPU/하드웨어** - ⚠️ 성능 차이

**동작 여부**: ✅ CPU에서도 동작 (느림)

**차이점**:
- **NVIDIA GPU**: 최적화됨, 빠름
- **AMD GPU**: 지원되지만 설정 필요
- **CPU만**: 매우 느림 (수십 분 소요 가능)

**권장사항**:
- GPU가 없어도 동작하지만, 실용적이지 않음
- GPU가 있으면 CUDA/cuDNN 버전 확인 필요

---

### 9. **설정 파일** - ✅ 복사 권장

**복사 권장 파일들**:
```
config_states/          # 사용자 설정
webui-user.sh          # 사용자 커스텀 설정
```

**차이점**:
- 설정 파일이 없으면 기본값 사용
- 복사하면 이전 설정 유지 가능

---

## 📋 복사 체크리스트

### ✅ **반드시 복사해야 할 것**
- [x] `stable-diffusion-webui/` 전체 폴더 (venv 제외)
- [x] `models/` 폴더 (모델 파일들)
- [x] `config_states/` (설정 유지하려면)
- [x] `webui-user.sh` (커스텀 설정이 있다면)

### ❌ **복사 불필요한 것**
- [ ] `venv/` 폴더 (자동 생성됨)
- [ ] `cache/` 폴더 (자동 생성됨)
- [ ] `outputs/` 폴더 (선택사항)
- [ ] `__pycache__/` 폴더들 (자동 생성됨)
- [ ] `.git/` 폴더 (선택사항)

---

## 🚀 복사 후 실행 방법

### 1. **프로젝트 복사**
```bash
# 방법 1: rsync 사용 (venv 제외)
rsync -av --exclude='venv' \
  --exclude='__pycache__' \
  --exclude='cache' \
  /원본경로/stable-diffusion-webui/ \
  /새경로/stable-diffusion-webui/

# 방법 2: tar 사용
cd /원본경로
tar --exclude='venv' \
    --exclude='__pycache__' \
    --exclude='cache' \
    -czf backup.tar.gz stable-diffusion-webui/
cd /새경로
tar -xzf backup.tar.gz
```

### 2. **실행 권한 부여**
```bash
cd /새경로/stable-diffusion-webui
chmod +x webui.sh
```

### 3. **실행**
```bash
./webui.sh
```

**자동으로 처리되는 것들**:
- ✅ Python 가상환경 생성
- ✅ 필요한 라이브러리 설치
- ✅ 경로 설정
- ✅ 모델 확인

---

## 🔧 문제 해결

### 문제 1: "Python not found"
```bash
# Python 설치 확인
python3 --version

# webui-user.sh에서 Python 경로 지정
python_cmd="python3.10"  # 또는 "python3"
```

### 문제 2: "Permission denied"
```bash
chmod +x webui.sh
chmod +x webui-user.sh
```

### 문제 3: "CUDA out of memory"
- GPU 메모리 부족
- `webui-user.sh`에서 메모리 최적화 옵션 추가:
```bash
export COMMANDLINE_ARGS="--medvram --opt-split-attention"
```

### 문제 4: "Model not found"
- `models/Stable-diffusion/` 폴더에 모델 파일이 있는지 확인
- 없으면 WebUI가 자동으로 다운로드 시도 (느릴 수 있음)

---

## 📊 복사 방법별 비교

| 방법 | 장점 | 단점 | 용량 |
|------|------|------|------|
| **전체 복사** | 설정 완벽 유지 | 용량 큼 (~13GB+) | 큼 |
| **venv 제외** | 권장 방법 | 첫 실행 시 설치 시간 | 중간 (~8GB) |
| **모델만 복사** | 최소 용량 | 설정 재구성 필요 | 작음 (~수백MB) |

---

## 💡 권장 복사 방법

### **방법 A: 완전 복사 (권장)**
```bash
# venv와 캐시만 제외하고 모두 복사
rsync -av \
  --exclude='venv' \
  --exclude='__pycache__' \
  --exclude='cache' \
  --exclude='outputs' \
  source/stable-diffusion-webui/ \
  dest/stable-diffusion-webui/
```
**용량**: ~8-10GB  
**장점**: 설정과 모델 모두 유지  
**단점**: 용량 큼

---

### **방법 B: 최소 복사 (용량 절약)**
```bash
# 코드와 모델만 복사
rsync -av \
  --exclude='venv' \
  --exclude='__pycache__' \
  --exclude='cache' \
  --exclude='outputs' \
  --exclude='.git' \
  source/stable-diffusion-webui/ \
  dest/stable-diffusion-webui/
```
**용량**: ~5-8GB  
**장점**: 용량 절약  
**단점**: 설정 재구성 필요

---

## 🎯 결론

### ✅ **동일하게 동작하는 것들**
- 코드 로직
- 웹 인터페이스
- 기능들
- 설정 (복사한 경우)

### ⚠️ **차이가 날 수 있는 것들**
- Python 버전
- 시스템 라이브러리
- GPU/하드웨어 성능
- 첫 실행 시간 (라이브러리 설치)

### 📝 **권장 사항**
1. **venv 폴더는 복사하지 말 것** (자동 생성됨)
2. **models 폴더는 복사할 것** (용량 허용 시)
3. **첫 실행 시 시간 여유** (라이브러리 설치)
4. **Python 버전 확인** (3.10.6 권장)

---

## 📚 추가 참고

- **공식 문서**: [Installation Guide](https://github.com/AUTOMATIC1111/stable-diffusion-webui/wiki/Install-and-Run-on-NVidia-GPUs)
- **문제 해결**: `webui.sh` 실행 시 에러 메시지 확인
- **성능 최적화**: GPU가 있으면 CUDA 설정 확인

---

**작성일**: 2026-02-20  
**버전**: 1.0

**요약**: 프로젝트를 복사해도 거의 동일하게 동작하지만, venv 폴더는 제외하고 복사하는 것을 권장합니다! 🚀
